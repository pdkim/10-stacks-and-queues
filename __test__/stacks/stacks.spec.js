'use strict';

const stack = require('../../lib/stacks/stacks.js');

describe('Stacks module should', () => {

  xit('add an element to the array following the LIFO method', () => {
    expect(null).toBeNull();
  });

  xit('remove an element in the array following the LIFO method', () => {
    expect(null).toBeNull();
  });

  xit('do something else cool', () => {
    expect(null).toBeNull();
  });
})