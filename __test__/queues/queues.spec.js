'use strict';

const queue = require('../../lib/queues/queues.js');

describe('Queues module should', () => {

  xit('add an element following FIFO guideline', () => {
    expect(null).toBeNull();
  });

  
  xit('remove an element following the FIFO guideline', () => {
    expect(null).toBeNull();
  });
});